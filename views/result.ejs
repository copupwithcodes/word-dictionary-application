<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    <title>Dictionary</title>
</head>
<body class="bg-light">
    <center>
        
        <p class="h2 mt-5 mb-4 text-primary">Word Dictionary</p>
        <form action="/" method="post">
            <div class="container" style="max-width: 300px;">
                <input type="text" name="word"  autocomplete ="off" placeholder="enter word" class="form-control">
                <button type="submit" class="btn btn-info mt-2">Search</button>
            </div>
        </form>
    </center>
    <div class="result" style="margin-left: 10%; margin-right: 20%;">
        <h4 class="mb-7">word:&nbsp;&nbsp;<%= word %></h4>
            <% for(let i=0; i<meanings.length; i++){  %>
                <p>
                    <h5 class="text-dark"><i> <%= meanings[i].partOfSpeech %> (definitions given below: ) </i></h5>
                    <ul>
                        <% for(let j=0; j< meanings[i].definitions.length; j++){  %>
                           <% if(j==5)  break;%>
                            <li> <%= meanings[i].definitions[j].definition; %> </li>
                        <%  }  %>
                    </ul>
                    <% if(meanings[i].synonyms.length >0){  %>
                        <h5 class="text-dark">Synonyms- </h5>
                        <ul>
                            <%  for(let k=0; k< meanings[i].synonyms.length; k++){  %>
                                    <li><%= meanings[i].synonyms[k] %></li>
                            <%  }  %>
                        </ul>
                    <%  }   %>
    
                    <% if(meanings[i].antonyms.length >0){  %>
                        <h5>Antonyms- </h5>
                        <ul>
                            <%  for(let k=0; k< meanings[i].antonyms.length; k++){  %>
                                    <li><%= meanings[i].antonyms[k] %></li>
    
                            <%  }  %>
                        </ul>
                    <%   }   %>
                </p>
            <%  }  %>
    </div>
</body>
</html>